# Map Frequency to an FFT Bin

## Goal
Bin spacing is `sample_rate / sample_count`. Compute spacing and the bin for the tone in `signal.npy`.

## Steps
1. Press Start → `./run`.
2. Finish the starter TODOs.
3. `./check` → press Done when it passes.
