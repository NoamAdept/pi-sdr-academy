# Vector data flow
Edit vector_flow.py. Build a float32 VectorSource with vlen=4, convert to uint8,
consume it in a VectorSink, and return text, item_size, and item_count.
Remember: one vector item contains four scalar floats.
