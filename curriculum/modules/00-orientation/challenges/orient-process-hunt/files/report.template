# Fill in ALL 5 lines. Do not leave blanks.
#
# process_number = the PID of the helper program
# full_command   = the full text of how it was started
# folder         = the folder that process is using
# notes_file     = the file path after --notes in the command
# secret_code    = the code inside that notes file

process_number=
full_command=
folder=
notes_file=
secret_code=
