INCIDENT 0xBEAC — Silent Port
=============================
Beacon failed health checks during the last lab image bake.

Symptoms students report:
  - "./service/beaconctl.sh start" errors or exits
  - curl to the health URL → connection refused

Do not rebuild anything. Fix the local config (and data path), start the
service on localhost, then read the flag from the live /health response.
