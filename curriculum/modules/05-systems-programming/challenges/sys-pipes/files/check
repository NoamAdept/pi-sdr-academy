#!/usr/bin/env python3
import subprocess, sys
from pathlib import Path

from pathlib import Path
import os
def award():
    print('CHECK_OK')

try:
    subprocess.run(['cc','-Wall','-Wextra','pipe_demo.c','-o','pipe_demo'],check=True); out=subprocess.check_output(['./pipe_demo'],text=True,timeout=3); assert 'samples=64' in out
except Exception as e:
    print("Not ready:", e); raise SystemExit(1)
award()
