# Translating signal chain
Edit signal_chain.py. The capture contains an AM channel centered at +2200 Hz.
Model a throttle at 8000 samples/s, translate/filter it, take magnitude, trim
filter edges, and find the non-DC envelope peak. No GNU Radio install is needed.
