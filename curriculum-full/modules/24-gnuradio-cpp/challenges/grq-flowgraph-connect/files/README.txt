# Flowgraph connections
Edit connect_flowgraph.py. Add has two input ports. Connect each vector source
to a distinct Endpoint(add, port), then connect add -> gain -> sink and run.
The sink's uint8 values decode to a short verification text.
