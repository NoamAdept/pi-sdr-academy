#!/usr/bin/env python3
import subprocess, sys
from pathlib import Path
import numpy as np

from pathlib import Path
import os
def award():
    print('CHECK_OK')

def vals(name):
    return dict(line.strip().split('=',1) for line in Path(name).read_text().splitlines() if '=' in line)
try:
    subprocess.run([sys.executable,'generate_data.py'],check=True)
    subprocess.run([sys.executable,'analyze.py'],check=True)
    a=np.load('samples.npy'); r=np.load('reference.npy'); assert a.shape==(2048,) and np.allclose(a,r,atol=1e-7)
except Exception as e:
    print("Not ready:", e); raise SystemExit(1)
award()
