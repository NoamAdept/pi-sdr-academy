#!/usr/bin/env python3
import subprocess, sys
from pathlib import Path

from pathlib import Path
import os
def award():
    print('CHECK_OK')

try:
    subprocess.run(['cc','-Wall','-Wextra','controller.c','-o','controller'],check=True); out=subprocess.check_output(['./controller'],text=True,timeout=3); assert 'child_ok' in out and 'child_status=0' in out
except Exception as e:
    print("Not ready:", e); raise SystemExit(1)
award()
